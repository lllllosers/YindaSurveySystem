# V1.0 多单位任务与成果生产验收矩阵

## 1. 验收目标

本矩阵用于 V1.0 发布前验证“总库分发任务—基层单位独立调查—成果回收—总库汇总”的生产工作流。

正式业务范围为附表2.1～2.14工程现状调查。附表1.1～1.15仍为预留业务，不纳入本轮生产验收。

## 2. 已自动化覆盖的核心场景

| 场景 | 预期结果 | 自动化依据 |
| --- | --- | --- |
| 同一项目/批次向两个管理单位分别分发任务 | 生成两个独立 task_uid，冻结各自分管范围 | Stage 7 多单位往返测试 |
| 两个管理单位在独立数据库接收任务 | 各子库仅建立自己的任务工作区和冻结范围 | Stage 7 / task workspace 测试 |
| 仅一个单位先返回成果 | 对应任务显示“已有成果返回”，另一个保持“待回收” | Stage 7.4 |
| 第二个单位后续返回成果 | 两个任务均显示已有成果返回 | Stage 7.4 |
| 多个 .ydresult 顺序导入同一项目/批次 | 数据在同一项目/批次中合并，不生成重复项目/批次 | Stage 7.1 / 7.4 |
| 来源追踪 | survey_record 保留 source_task_uid + source_management_scope_uid | Stage 7.1 |
| 成果接收历史 | 可按管理单位查看，按接收时间排序 | Stage 7.2 |
| 任务分发历史 | 可查看管理单位、范围数、已回收记录数、回收状态 | Stage 7.3 |
| 工程台账汇总 | 多单位导入后同时可见 | Stage 7.1 / 7.4 |
| 数据查询汇总 | 多单位导入后同时可见 | Stage 7.1 / 7.4 |
| 首页完成情况 | 随成果回收从 1 条更新为 2 条，并按单位汇总 | Stage 7.4 |
| 相同成果包重复导入 | 幂等，不重复写入，不额外创建备份 | Stage 7.1 / 7.4 |
| 任务下发后当前分管主数据变化 | 历史成果仍按下发时冻结范围验收 | Stage 14.4.4 端到端测试 |
| 错误 task_uid / scope_uid 成果 | 预检/导入拒绝 | Stage 14.4.4 / scope authority 测试 |
| 导入前数据库备份 | 首次正式导入前创建安全备份 | result import / Stage 7 |

## 3. 人工生产验收

自动测试全部通过后，使用程序界面进行一次人工模拟。

### A. 总库准备

1. 新建或选择一个测试项目和调查批次。
2. 确认基础资料、管理单位、渠系和分管范围完整。
3. 在“任务分发”中分别为两个不同管理单位生成任务包。
4. 检查“已分发任务”出现两条记录，初始均为“待回收”。

### B. 两个独立调查端

1. 使用两个独立测试数据库分别接收两个任务包。
2. 每个调查端至少完成一条附表2工程调查记录。
3. 分别生成调查成果包。

### C. 分阶段回收

1. 总库先接收第一个成果包。
2. 检查：
   - 对应任务变为“已有成果返回”；
   - 另一任务仍为“待回收”；
   - 成果接收历史只有一个成果包；
   - 工程台账、数据查询、首页统计只增加第一个单位的数据。
3. 再接收第二个成果包。
4. 检查：
   - 两个任务均显示“已有成果返回”；
   - 成果接收历史有两个成果包；
   - 工程台账、数据查询、首页统计均包含两个单位的数据。

### D. 重复与异常操作

1. 再次选择已经导入过的同一成果包。
2. 系统不得重复生成工程对象、调查记录、分项评价或影像。
3. 用错误项目/批次或被篡改的测试成果包执行预检，应被阻止。
4. 检查导入前备份文件能够正常生成。

## 4. V1.0 发布判定

满足以下条件后，Stage 7 可判定完成：

- Stage 7.1～7.4 针对性测试全部通过；
- 数据交换相关回归测试全部通过；
- `run_tests.bat` 全量通过；
- 上述人工生产验收 A～D 无阻断性问题；
- 工程台账、数据查询、首页、任务分发历史、成果接收历史对同一批次数据的统计口径一致。

本矩阵不把“已有成果返回”等同于“任务整体完成”。当前系统没有任务级“应调查记录总量”的权威基准，因此 V1.0 仅展示已回收记录数量和是否已有成果返回。

## 5. 验收结果

验收日期：2026-09-19。

验收结论：**通过**。

已完成并确认：

- Stage 7.1～7.4 针对性自动化测试全部通过；
- 数据交换与冻结任务权威校验相关回归全部通过；
- `run_tests.bat` 全量测试通过；
- 两个独立调查端、多管理单位、分阶段成果回收人工模拟生产验收完成；
- 工程台账、数据查询、首页、任务分发历史和成果接收历史汇总口径一致；
- 相同成果包重复导入未产生重复业务数据；
- 正式成果导入前备份机制正常。

因此 Stage 7 生产验证正式收口，项目进入 V1.0.0 发布阶段。

---

## 6. V1.1.0 三级任务与成果链补充验收

### 6.1 验收范围

V1.1.0 在既有“中心直接向水管所分发”的两级链基础上，新增原生：

```text
中心 → 基层处 → 水管所 → 基层处 → 中心
```

本轮补充验收使用中心、基层处、水管所A、水管所B四套完全独立 `local_data`。

### 6.2 自动化验收

| 场景 | 预期结果 | 验收依据 |
| --- | --- | --- |
| 中心生成处级父任务 T1 | 冻结该处选中所级 scope 集合 | Stage 4.2A / 4.2B |
| 处从 T1 派生 T2/T3 | 子任务范围不得超出 T1 | Stage 4.3 |
| 所级成果回处 | 处按自己下发的 child task 验收 | Stage 4.4A / 4.5C |
| 处级自动汇总 | 自动选择 T1 授权范围内合法 completed 记录 | Stage 4.4B |
| 处向中心提交 | submission_task_uid=T1 | Stage 4.4A / 4.5B |
| 记录来源保留 | source_task_uid 仍为 T2/T3 | Stage 4.4A / 4.5C |
| 中心没有 T2/T3 issued history | 仍可按自己下发的 T1 验收处级汇总 | Stage 4.5C |
| 中心直接收到 T2/T3 所级成果 | 因无对应授权拒绝 | 人工异常路径验收 |
| 旧中心→所流程 | 继续可用 | Stage 4.5A 回归 |

### 6.3 人工生产验收

验收日期：2026-09-20。

实际完成：

1. 中心端选择“整个基层处”生成 T1；
2. 处端独立数据库接收 T1；
3. 处端分别向两个水管所生成 T2、T3；
4. 两个水管所分别使用独立数据库接收并完成调查记录；
5. 所A、所B分别生成 `.ydresult` 回处；
6. 处端依次预检和导入两个所成果；
7. 处端当前 workspace 仍保持 T1；
8. 处级成果提交界面自动显示父任务汇总模式；
9. 即使上方普通筛选只选一个水管所，父任务 `.ydresult` 汇总仍保持 T1 完整授权范围；
10. 处生成 `submission_task_uid=T1` 的汇总成果；
11. 中心在没有 T2/T3 issued history 的情况下预检和正式导入成功；
12. 中心最终记录仍保留 T2/T3 的真实 `source_task_uid`；
13. 工程台账、数据查询和成果接收结果一致；
14. 水管所端不能继续向下分发任务；
15. 旧中心直接向水管所分发流程仍正常。

### 6.4 验收结论

**通过。**

V1.1.0 原生三级任务与成果链已完成自动化和人工生产验收，可进入正式发布收口。

